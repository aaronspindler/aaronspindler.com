# Development dependencies
# Install with: pip install -r requirements/dev.txt

# Include production dependencies
-r base.txt

# Pre-commit framework for git hooks
pre-commit==4.3.0

# Linting and formatting
ruff==0.14.1

# Security auditing (used in CI/CD)
safety==3.6.2
pip-audit==2.9.0
