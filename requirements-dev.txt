# Development dependencies
# Install with: pip install -r requirements-dev.txt

# Include production dependencies
-r requirements.txt

# Pre-commit framework for git hooks
pre-commit==4.3.0

# Linting and formatting (Ruff does it all!)
ruff==0.8.4

# Type checking (optional - used in CI/CD but not in local hooks)
mypy==1.13.0
django-stubs==5.1.1

# Security auditing (optional - used in CI/CD)
safety==3.2.11
pip-audit==2.7.3

# Testing utilities (if not in main requirements)
coverage==7.6.9
