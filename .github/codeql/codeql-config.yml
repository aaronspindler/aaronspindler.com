name: "CodeQL Config"

# Paths to ignore during the analysis
paths-ignore:
  - projects/**

# You can also specify which paths to include (optional)
# paths:
#   - accounts
#   - blog
#   - config
#   - pages
#   - photos

# Query suites to use
queries:
  - uses: security-extended
  - uses: security-and-quality
